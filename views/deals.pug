//- ** This is a sample of a pug template and how it uses the data passed to it from index.js.

doctype html
html 
    head 
        title= `${title}`
        meta(name="viewport" content="width=device-width, initial-scale=1")
        link(rel="stylesheet", href="/css/style.css")
    body    
        h1 Deals 

         a(href='/update-cobj') Add a new record
         
        .cards 
            each contact in data
                .card 
                    h2.card__name #{contact.properties.dealname}  
                    p.card__email Amount: #{contact.properties.amount}
                    p.card__email State: #{contact.properties.dealstage}